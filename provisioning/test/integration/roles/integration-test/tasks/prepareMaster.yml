---
- name: Start application on sparkMaster
  shell: cd /home/{{ user }}/applications; screen -L -d -m ./run-application.sh ./protocols-statistics/protocols_statistics.py -iz producer:2181 -it ipfix.entry -oh consumer:20101; sleep 1;

- name: Wait 25seconds
  pause: seconds={{ time_app_start }}
